services:
  db:
    image: postgres:17
    container_name: aubonheur_db
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: